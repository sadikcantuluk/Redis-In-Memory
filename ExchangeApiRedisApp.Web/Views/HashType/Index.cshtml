@{
    ViewData["Title"] = "Index";
}

@model List<string>

<h1>Index</h1>

<form method="post">
    <ul>
        @foreach (var item in Model)
        {
            <li>
                @item <button type="submit" asp-controller="HashType" asp-action="Delete" asp-route-name="@item" class="btn btn-danger">Sil</button>
            </li>
        }
    </ul>
</form>

<a class="btn btn-primary" href="HashType/Add">Ekle</a>









